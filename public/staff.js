(function(){const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const units=document.getElementById('unitCards'),metrics=document.getElementById('staffMetrics'),message=document.getElementById('staffMessage');const roleLabels={viewer:'Viewer, read-only',officer:'Officer, operational actions',administrator:'Administrator, full assigned-unit control'};function card(unit,staff){let action='<span class="staff-restricted">Workspace access is ready. Actions remain restricted to your assigned role.</span>';if(unit.id==='student-support')action=`<a class="btn" href="/support-admin">Open Student Support queue</a>`;else if(unit.id==='payroll'&&staff.departments.length&&staff.sections.includes('payroll'))action=`<a class="btn" href="/payroll/${encodeURIComponent(staff.departments[0])}">Open Payroll Portal</a>`;else if(unit.id==='auditor'&&staff.departments.length&&staff.sections.includes('auditor'))action=`<a class="btn" href="/auditor/${encodeURIComponent(staff.departments[0])}">Open Auditor's Portal</a>`;return `<article class="staff-unit-card"><span class="service-tag">${esc(roleLabels[staff.role]||staff.role)}</span><h3>${esc(unit.label)}</h3><p>${esc(unit.summary)}</p>${action}</article>`;}async function load(){try{const response=await fetch('/api/staff/me');const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Could not load the staff workspace.');const staff=data.staff;document.getElementById('welcome').textContent=`Welcome, ${staff.name}`;document.getElementById('accessSummary').textContent=`${roleLabels[staff.role]||staff.role}. You have access to ${staff.units.length} functional unit${staff.units.length===1?'':'s'}.`;metrics.innerHTML=staff.units.some(unit=>unit.id==='student-support')?`<article class="staff-metric"><span>Open Student Support tickets</span><strong>${esc(data.metrics.openSupportTickets)}</strong></article>`:'';units.innerHTML=staff.units.map(unit=>card(unit,staff)).join('')||'<div class="resource-section">No functional-unit access has been assigned to this account.</div>';}catch(error){message.textContent=error.message||'Could not load the staff workspace.';message.className='status show bad';}}load();})();
